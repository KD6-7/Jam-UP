# Base URL of the FastAPI backend, e.g. https://jam-up-backend.onrender.com
NEXT_PUBLIC_API_URL=

# OAuth 2.0 Client ID from Google Cloud Console (same one as the backend's
# GOOGLE_CLIENT_ID). Public by design — it identifies the app, not a secret.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
